import { ReactNode } from 'react';
import { ITextInputProps } from './TextInput.types';
declare const TextInput: ({ borderColor, borderRadius, bottom, center, label, labelError, left, multiline, padding, right, rows, style, styleLabel, styleTextInput, top, ...props }: ITextInputProps) => ReactNode;
export default TextInput;
